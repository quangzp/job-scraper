services:
  postgres:
    image: postgres:16-alpine
    container_name: job_platform_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-job_platform}
      POSTGRES_USER: ${POSTGRES_USER:-job_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-job_password}
      TZ: ${TZ:-Asia/Saigon}
    ports:
      - "${POSTGRES_EXPOSE_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-job_user} -d ${POSTGRES_DB:-job_platform}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
